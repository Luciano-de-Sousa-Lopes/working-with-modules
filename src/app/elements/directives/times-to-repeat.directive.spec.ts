import { TimesToRepeatDirective } from './times-to-repeat.directive';

describe('TimesToRepeatDirective', () => {
  it('should create an instance', () => {
    const directive = new TimesToRepeatDirective();
    expect(directive).toBeTruthy();
  });
});
